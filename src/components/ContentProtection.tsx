/**
 * ContentProtection Component
 * Temporarily disabled — right-click, text selection, screenshots, and
 * keyboard shortcuts are all allowed so the site can be reviewed/captured.
 * Re-enable by restoring the previous implementation when needed.
 */
const ContentProtection = () => {
  return null;
};

export default ContentProtection;
